grumphp:
    fixer:
        fix_by_default: true
    tasks:
        composer: ~
        jsonlint:
            detect_key_conflicts: true
        yamllint: ~
        phpcs:
            standard: phpcs.xml.dist
        phpstan:
            configuration: phpstan.neon
